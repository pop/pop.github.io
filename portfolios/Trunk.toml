[build]
target = "index.html"
dist = "dist"
# Emit relative asset links (./main-xxx.css, ./project-os.js, …) so the bundle
# works under any path — served canonically at pages.elijah.run/portfolios/, and
# transparently at games.elijah.run/ via the legacy-host rewrite.
public_url = "./"
