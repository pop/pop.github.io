name = "blog-editor-oauth"
main = "build/worker/shim.mjs"
compatibility_date = "2024-01-01"

[build]
command = "worker-build --release"

# Set these secrets with: wrangler secret put GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET
