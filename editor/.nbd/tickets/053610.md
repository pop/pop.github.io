+++
title = "Testing: write unit tests for pure functions"
priority = 5
status = "done"
ticket_type = "task"
dependencies = ["d25a02"]
+++
Add #[cfg(test)] modules for pure functions (no browser/WASM runtime needed).\n\nTests needed:\n- models/post.rs: strip_frontmatter (with/without frontmatter, empty, unclosed), render_markdown (basic, GFM features, frontmatter stripping)\n- services/github.rs: decode_github_content (valid base64, base64 with whitespace, invalid, empty)\n- components/editor.rs (or utils.rs): slug_from_path, title_from_slug, sanitize_filename, char_pos_to_byte_offset, parent_dir, generate_template\n- components/dashboard.rs (or utils.rs): format_size